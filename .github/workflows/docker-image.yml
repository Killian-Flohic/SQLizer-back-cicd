name: Docker Image CI custom

on:
  push:
    branches: [ "Pré-Prod" ]
  pull_request:
    branches: [ "Pré-Prod" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker compose up -d
